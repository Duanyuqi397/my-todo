import React from "react";
import { Header } from "../Header/Header";

export const RemindList: React.FC = () => {
    return (
        <div>
            <Header title="日程提醒" methodName="add"/>
        </div>
    )
}